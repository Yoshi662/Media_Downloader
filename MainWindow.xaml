<Window x:Class="Media_Downloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Media_Downloader"
        mc:Ignorable="d"
        Title="Media Downloader" Height="450" Width="800">
    
    <DockPanel>
        <Menu Name="MainMenu" IsMainMenu="True" DockPanel.Dock="Top">
            <MenuItem Header="Opciones">
                <MenuItem Header="Actualizar"/>
            </MenuItem>
            <MenuItem Header="Presets - Need translation">
                <MenuItem Header="Añadir Preset"/>
                <MenuItem Header="Preset Example">
                    <MenuItem Header="Usar"/>
                    <MenuItem Header="Borrar"/>
                </MenuItem>
            </MenuItem>

        </Menu>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5" VerticalAlignment="Center" >
            <Label Content="URL:"/>
            <TextBox Name="txt_URL" MinWidth="500" Margin="2"/>
            <ComboBox Name="cb_Formats" Margin="2" MinWidth="70"/>

        </StackPanel>

        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="5"  >
            <RadioButton GroupName="FormatOptions" Name="rb_Video" Content="Video" IsChecked="True" Click="Seleccion_click" />
            <RadioButton GroupName="FormatOptions" Name="rb_Audio" Content="Audio" Click="Seleccion_click" Margin="10,0,0,0" />
        </StackPanel>

        <Grid Name="MainGrid" Margin="10" DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Vertical">

                <!--Playlist Options-->
                <GroupBox Header="Opciones de lista" Grid.Column="0">
                    <StackPanel Orientation="Vertical">
                        <CheckBox Name="isPlayList" Content="Playlist" Margin="0,5,0,3"/>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Name="chk_startsAt" Content="Empezar por:" Click="Chk_startsAt_Click" Margin="4"/>
                            <TextBox Name="txt_StartsAt"  MinWidth="75" Width="Auto" Margin="4"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <CheckBox Name="chk_endsAt" Content="Terminar por:" Click="Chk_endsAt_Click"  Margin="4" />
                            <TextBox Name="txt_endsAt"  MinWidth="75" Width="Auto" Margin="4"/>
                        </StackPanel>
                    </StackPanel>
                </GroupBox>

                <!--SubtitleOptions-->
                <GroupBox Header="Subtitulos">
                    <StackPanel Orientation="Vertical">
                        <CheckBox Name="chk_Addsubsfile" Content="Descargar Subtitulos" Click="Chk_Addsubsfile_Click" Margin="0,4" ToolTip="Descargar un archivo con subtitulos (.srt)"/>
                        <CheckBox Name="chk_Embedsubs" Content="Integrar Subtitulos" Click="Chk_Addsubsfile_Click" Margin="0,4" ToolTip="Integrara los subitulos dentro del video (Solo Ciertos formatos)"/>
                    </StackPanel>
                </GroupBox>
            </StackPanel>
        </Grid>

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" VerticalAlignment="Bottom" HorizontalAlignment="Right"  Margin="0,0,10,10">
            <Button Content="Actualizar" Click="Btn_Actualizar" MinHeight="25" MinWidth="80" Margin="5,5,5,5"/>
            <Button Content="Descargar" Click="Btn_Descargar" MinHeight="25" MinWidth="80" Margin="5,5,5,5" />
        </StackPanel>
        <Expander Header="(Avanzado) - Ver Comando final" VerticalAlignment="Bottom" DockPanel.Dock="Bottom" Margin="5">
            <TextBox Name="txt_Comando" Margin="4" Text="{Binding Comando}"/>
        </Expander>
    </DockPanel>
</Window>
